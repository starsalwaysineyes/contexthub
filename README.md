# contexthub

ContextHub is being reinitialized for the phase-1 `ctx://` cloud filesystem direction.

Current phase-1 focus:
- cloud directory/workspace creation
- `ctx_cli`
- `ctx://` path semantics
- filesystem-like verbs first

The old record-first implementation history is preserved on branch `feat`.
